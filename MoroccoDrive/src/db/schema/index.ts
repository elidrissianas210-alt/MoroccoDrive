/**
 * Database schema definitions will be added in future domain tasks.
 *
 * This empty schema is intentional: the Drizzle foundation must not create
 * application tables before their domain requirements are defined.
 */
export const schema = {};
